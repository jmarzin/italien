%h1 Liste des mots et expressions
%p#notice= notice
-if user_signed_in?
  -user_id = current_user.id
-else
  -user_id = User.where(admin: true).first.id
%table#mot
  %tr
    %th Mot directeur
    %th Francais
    %th Italien
    %th Compteur
    %th Créé le
    %th Modifié le
    %th
    -if @peut_corriger
      %th
    -if @peut_supprimer
      %th

  - @mots.each do |mot|
    %tr
      %td= mot.mot_directeur
      %td= mot.francais
      %td= mot.italien
      %td= mot.scores_mots.where(user_id: user_id, mot_id: mot.id).first.compteur
      %td= mot.created_at.strftime('%Y/%m/%d')
      %td= mot.updated_at.strftime('%Y/%m/%d')
      %td= link_to 'Voir', mot
      -if @peut_corriger
        %td= link_to 'Corr', edit_mot_path(mot)
      -if @peut_supprimer
        %td= link_to 'Supp', mot, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
= paginate @mots
-if @peut_supprimer
  = link_to 'Nouveau', new_mot_path